services:
  - type: web
    name: devshowcase-api
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: node src/app.js
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production